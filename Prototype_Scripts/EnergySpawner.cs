using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EnergySpawner : MonoBehaviour {

	public Transform[] spawnPoints; 

	// Use this for initialization
	void Start () {
		
	}

	// Update is called once per frame
	void Update () {
		//GameObject Spawnpoint = GameObject.Spawn[Random.Range(0,EnergySpawner.Length)];
		//	Instantiate (SlimeSpikey, spawnPoints [spawnPointIndex].position, Quaternion.identity);
	

	
}
}
